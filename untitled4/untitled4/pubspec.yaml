name: flutter_assignment_project_final_ouch
description: A new Flutter project using Ouch illustrations.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/login_ouch.png
    - assets/sign_up.png
    - assets/phone.png
    - assets/profile.jpg
    - assets/pdf_icon.jpg